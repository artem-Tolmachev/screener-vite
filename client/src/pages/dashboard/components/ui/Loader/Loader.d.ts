import { MarketData } from "@/pages/dashboard/types";
interface Props {
    tick: MarketData[];
    panelIndex: number;
}
export default function Loader({ tick, panelIndex }: Props): import("react/jsx-runtime").JSX.Element;
export {};
