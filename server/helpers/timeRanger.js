// export default function timeRanger (orderBooks){ 
//   const timeRange = 1800000;
//   const filtred = new Map([...orderBooks].filter(([a, b]) => b.time <= Date.now() - timeRange));
//   return filtred;
// }